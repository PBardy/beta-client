import { View, Text } from 'react-native';
import React from 'react';

type Props = {};

const UserProductsSelector = (props: Props) => {
  return (
    <View>
      <Text>UserProductsSelector</Text>
    </View>
  );
};

export default UserProductsSelector;
