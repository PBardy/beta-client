import { View, Text } from 'react-native';
import React from 'react';

type Props = {};

const ProductsSelector = (props: Props) => {
  return (
    <View>
      <Text>ProductsSelector</Text>
    </View>
  );
};

export default ProductsSelector;
