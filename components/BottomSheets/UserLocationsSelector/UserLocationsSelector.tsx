import { View, Text } from 'react-native';
import React from 'react';

type Props = {};

const UserLocationsSelector = (props: Props) => {
  return (
    <View>
      <Text>UserLocationsSelector</Text>
    </View>
  );
};

export default UserLocationsSelector;
