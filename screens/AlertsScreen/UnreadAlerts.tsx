import { View, Text } from 'react-native';
import React from 'react';

type Props = {};

const UnreadAlerts = (props: Props) => {
  return (
    <View>
      <Text>UnreadAlerts</Text>
    </View>
  );
};

export default UnreadAlerts;
