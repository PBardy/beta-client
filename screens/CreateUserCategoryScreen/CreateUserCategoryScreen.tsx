import { View, Text } from 'react-native';
import React from 'react';

type Props = {};

const CreateUserCategoryScreen = (props: Props) => {
  return (
    <View>
      <Text>CreateUserCategoryScreen</Text>
    </View>
  );
};

export default CreateUserCategoryScreen;
