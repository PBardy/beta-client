import { View, Text } from 'react-native';
import React from 'react';

type Props = {};

const CreateUserLocationScreen = (props: Props) => {
  return (
    <View>
      <Text>CreateUserLocationScreen</Text>
    </View>
  );
};

export default CreateUserLocationScreen;
